import * as React from 'react';
import { StandaloneWeekViewProps } from "./WeekView.types.js";
/**
 * A Week View that can be used outside of the Event Calendar.
 */
declare const StandaloneWeekView: StandaloneWeekViewComponent;
export { StandaloneWeekView };
interface StandaloneWeekViewComponent {
  <TEvent extends object, TResource extends object>(props: StandaloneWeekViewProps<TEvent, TResource> & {
    ref?: React.ForwardedRef<HTMLDivElement>;
  }): React.JSX.Element;
  propTypes?: any;
}