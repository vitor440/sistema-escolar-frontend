import * as React from 'react';
import { EventCalendarProps } from "./EventCalendar.types.js";
declare const EventCalendar: EventCalendarComponent;
export { EventCalendar };
interface EventCalendarComponent {
  <TEvent extends object, TResource extends object>(props: EventCalendarProps<TEvent, TResource> & {
    ref?: React.ForwardedRef<HTMLDivElement>;
  }): React.JSX.Element;
  propTypes?: any;
}