import * as React from 'react';
import { ErrorContainerClasses } from "./error-container/errorContainerClasses.js";
import { EventSkeletonClasses } from "./event-skeleton/eventSkeletonClasses.js";
/**
 * Styling injected by the consuming product into the shared internal components
 * (e.g. `ErrorContainer`, `EventSkeleton`) so they stay decoupled from any
 * product-specific styled context.
 */
export interface SharedComponentsStyledContextValue {
  classes: ErrorContainerClasses & EventSkeletonClasses;
}
export declare const SharedComponentsStyledContext: React.Context<SharedComponentsStyledContextValue | null>;
export declare function useSharedComponentsStyledContext(): SharedComponentsStyledContextValue;