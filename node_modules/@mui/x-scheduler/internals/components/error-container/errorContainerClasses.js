"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.errorContainerSlots = exports.errorContainerClassKeys = void 0;
// Shared so every product that hosts the `ErrorContainer` derives the same keys.
// A rename here breaks the build in every product instead of silently dropping a CSS class.
const errorContainerClassKeys = exports.errorContainerClassKeys = ['errorContainer', 'errorAlert', 'errorMessage'];
const errorContainerSlots = exports.errorContainerSlots = {
  errorContainer: ['errorContainer'],
  errorAlert: ['errorAlert'],
  errorMessage: ['errorMessage']
};