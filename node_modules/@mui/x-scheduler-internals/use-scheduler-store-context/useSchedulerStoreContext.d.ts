import * as React from 'react';
import { SchedulerParameters, SchedulerState, SchedulerStore } from "../internals/utils/SchedulerStore/index.js";
export interface SchedulerStoreInContext<TEvent extends object, TResource extends object> extends SchedulerStore<any, any, SchedulerState, SchedulerParameters<TEvent, TResource>> {
  update: any;
}
export declare const SchedulerStoreContext: React.Context<SchedulerStoreInContext<any, any> | null>;
export declare function useSchedulerStoreContext<TEvent extends object, TResource extends object>(optional: true): SchedulerStoreInContext<TEvent, TResource> | null;
export declare function useSchedulerStoreContext<TEvent extends object, TResource extends object>(optional?: false): SchedulerStoreInContext<TEvent, TResource>;