"use strict";
'use client';

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useCalendarGridGetDateFromPositionInColumn = useCalendarGridGetDateFromPositionInColumn;
var React = _interopRequireWildcard(require("react"));
var _useAdapterContext = require("../../use-adapter-context");
var _CalendarGridTimeColumnContext = require("../time-column/CalendarGridTimeColumnContext");
function useCalendarGridGetDateFromPositionInColumn(parameters) {
  const {
    elementRef,
    snapMinutes
  } = parameters;
  const adapter = (0, _useAdapterContext.useAdapterContext)();
  const {
    getCursorPositionInElementMs,
    start
  } = (0, _CalendarGridTimeColumnContext.useCalendarGridTimeColumnContext)();
  return React.useCallback(clientY => {
    const offsetMs = getCursorPositionInElementMs({
      input: {
        clientY
      },
      elementRef
    });
    const anchor = adapter.addMilliseconds(start, offsetMs);
    return adapter.addMinutes(anchor, -(adapter.getMinutes(anchor) % snapMinutes));
  }, [adapter, getCursorPositionInElementMs, elementRef, snapMinutes, start]);
}