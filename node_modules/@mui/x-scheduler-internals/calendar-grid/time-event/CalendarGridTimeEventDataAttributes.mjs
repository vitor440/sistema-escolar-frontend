export let CalendarGridTimeEventDataAttributes = /*#__PURE__*/function (CalendarGridTimeEventDataAttributes) {
  /**
   * Present when the event start date is in the past.
   */
  CalendarGridTimeEventDataAttributes["started"] = "data-started";
  /**
   * Present when the event end date is in the past.
   */
  CalendarGridTimeEventDataAttributes["ended"] = "data-ended";
  /**
   * Present when the event is being dragged.
   */
  CalendarGridTimeEventDataAttributes["dragging"] = "data-dragging";
  /**
   * Present when the event is being resized.
   */
  CalendarGridTimeEventDataAttributes["resizing"] = "data-resizing";
  return CalendarGridTimeEventDataAttributes;
}({});