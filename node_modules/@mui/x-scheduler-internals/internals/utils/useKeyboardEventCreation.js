"use strict";
'use client';

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useKeyboardEventCreation = useKeyboardEventCreation;
var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/extends"));
var _store = require("@base-ui/utils/store");
var _useSchedulerStoreContext = require("../../use-scheduler-store-context");
var _schedulerSelectors = require("../../scheduler-selectors");
/**
 * Hook that returns a function to trigger event creation via keyboard (Enter key).
 * Returns `undefined` when event creation is disabled.
 *
 * This is the keyboard counterpart of `useEventCreation` (which handles mouse clicks).
 * Both call `store.setOccurrencePlaceholder` with the same `type: 'creation'` placeholder.
 */
function useKeyboardEventCreation(getCreationPlaceholder) {
  const store = (0, _useSchedulerStoreContext.useSchedulerStoreContext)();
  const creationConfig = (0, _store.useStore)(store, _schedulerSelectors.schedulerEventSelectors.creationConfig);
  if (creationConfig === false) {
    return undefined;
  }
  return () => {
    store.setOccurrencePlaceholder((0, _extends2.default)({
      type: 'creation'
    }, getCreationPlaceholder({
      creationConfig
    })));
  };
}