"use strict";
'use client';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useEvent = useEvent;
var _store = require("@base-ui/utils/store");
var _useSchedulerStoreContext = require("../../use-scheduler-store-context/useSchedulerStoreContext");
var _schedulerSelectors = require("../../scheduler-selectors");
function useEvent(parameters) {
  const {
    start,
    end,
    eventId
  } = parameters;
  const store = (0, _useSchedulerStoreContext.useSchedulerStoreContext)();
  const started = (0, _store.useStore)(store, _schedulerSelectors.schedulerOccurrenceSelectors.isStarted, start);
  const ended = (0, _store.useStore)(store, _schedulerSelectors.schedulerOccurrenceSelectors.isEnded, end);
  const editing = (0, _store.useStore)(store, _schedulerSelectors.schedulerOtherSelectors.isEditedEvent, eventId);
  return {
    state: {
      started,
      ended,
      editing
    }
  };
}