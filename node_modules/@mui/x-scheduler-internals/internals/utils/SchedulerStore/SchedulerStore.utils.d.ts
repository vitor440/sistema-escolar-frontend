import { TemporalTimezone } from "../../../base-ui-copy/types/index.js";
import { SchedulerProcessedEvent, SchedulerEventId, SchedulerOccurrencePlaceholder, SchedulerResource, SchedulerEventModelStructure, SchedulerResourceModelStructure, SchedulerEvent, SchedulerEventCreationProperties, SchedulerEventUpdatedProperties } from "../../../models/index.js";
import { Adapter } from "../../../use-adapter/useAdapter.types.js";
import { SchedulerParameters, SchedulerState } from "./SchedulerStore.types.js";
import { SchedulerRecurringEventsPluginInterface } from "../../plugins/SchedulerRecurringEventsPlugin.types.js";
/**
 * Determines if the occurrence placeholder has changed in a meaningful way that requires updating the store.
 */
export declare function shouldUpdateOccurrencePlaceholder(adapter: Adapter, previous: SchedulerOccurrencePlaceholder | null, next: SchedulerOccurrencePlaceholder | null): boolean;
export declare const DEFAULT_EVENT_MODEL_STRUCTURE: SchedulerEventModelStructure<any>;
/**
 * Converts an event model to a processed event using the provided model structure.
 */
export declare function getProcessedEventFromModel<TEvent extends object>(model: TEvent, adapter: Adapter, eventModelStructure: SchedulerEventModelStructure<TEvent> | undefined, displayTimezone: TemporalTimezone, recurringEventsPlugin?: SchedulerRecurringEventsPluginInterface | null): SchedulerProcessedEvent;
/**
 * Updates an event model based on the provided changes and model structure.
 * Converts internal date objects (`TemporalSupportedObject`) to strings
 * before applying them to the user's model, because `SchedulerEvent` date
 * fields are strings. Respects the original string format (instant vs wall-time).
 */
export declare function getUpdatedEventModelFromChanges<TEvent extends object>(oldModel: TEvent, changes: SchedulerEventUpdatedProperties, eventModelStructure: SchedulerEventModelStructure<TEvent> | undefined, adapter: Adapter, originalBuiltInModel: SchedulerEvent): TEvent;
/**
 * Create an event model from a processed event using the provided model structure.
 */
export declare function createEventModel<TEvent extends object>(event: SchedulerEventCreationProperties, eventModelStructure: SchedulerEventModelStructure<TEvent> | undefined, adapter: Adapter): {
  id: string;
  model: TEvent;
};
/**
 * Converts a resource model to a processed resource using the provided model structure.
 */
export declare function getProcessedResourceFromModel<TResource extends object>(resource: TResource, resourceModelStructure: SchedulerResourceModelStructure<TResource> | undefined): SchedulerResource;
/**
 * Throws if the resolved event id is missing.
 */
export declare function checkSchedulerEventIdIsValid(id: SchedulerEventId, event: object): void;
export declare function buildEventsState<TEvent extends object, TResource extends object>(parameters: Pick<SchedulerParameters<TEvent, TResource>, 'events' | 'eventModelStructure'>, adapter: Adapter, displayTimezone: TemporalTimezone, recurringEventsPlugin?: SchedulerRecurringEventsPluginInterface | null): Pick<SchedulerState<TEvent>, 'eventIdList' | 'eventModelLookup' | 'processedEventLookup' | 'eventModelStructure' | 'eventModelList'>;
export declare function buildResourcesState<TEvent extends object, TResource extends object>(parameters: Pick<SchedulerParameters<TEvent, TResource>, 'resources' | 'resourceModelStructure'>): Pick<SchedulerState<TEvent>, 'resourceIdList' | 'processedResourceLookup' | 'resourceModelStructure' | 'resourceChildrenIdLookup'>;