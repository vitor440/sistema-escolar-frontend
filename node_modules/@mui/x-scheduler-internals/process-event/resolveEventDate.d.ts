import type { TemporalSupportedObject, TemporalTimezone } from "../base-ui-copy/types/index.js";
import type { Adapter } from "../use-adapter/index.js";
import type { SchedulerEventId } from "../models/index.js";
/**
 * Resolves an event date string to a `TemporalSupportedObject`.
 *
 * - Strings ending with `"Z"` are treated as instants (UTC).
 * - Strings without `"Z"` are treated as wall-time and interpreted in `dataTimezone`.
 */
export declare function resolveEventDate(value: string, dataTimezone: TemporalTimezone, adapter: Adapter, eventId?: SchedulerEventId): TemporalSupportedObject;