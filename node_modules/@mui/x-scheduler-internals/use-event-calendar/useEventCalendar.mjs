'use client';

import { useIsoLayoutEffect } from '@base-ui/utils/useIsoLayoutEffect';
import { useDisposable } from '@mui/x-internals/useDisposable';
import { useAdapter } from "../use-adapter/useAdapter.mjs";
import { EventCalendarStore } from "./EventCalendarStore.mjs";
export function useEventCalendar(parameters, StoreClass) {
  const adapter = useAdapter(parameters.dateLocale);
  const ResolvedStoreClass = StoreClass ?? EventCalendarStore;
  const store = useDisposable(() => new ResolvedStoreClass(parameters, adapter));
  useIsoLayoutEffect(() => store.updateStateFromParameters(parameters, adapter), [store, adapter, parameters]);
  return store;
}