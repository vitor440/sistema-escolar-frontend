import * as React from 'react';
import { EventCalendarParameters, EventCalendarStoreConstructor } from "../use-event-calendar/index.js";
export declare function EventCalendarProvider<TEvent extends object, TResource extends object>(props: EventCalendarProvider.Props<TEvent, TResource>): import("react/jsx-runtime").JSX.Element;
export declare namespace EventCalendarProvider {
  interface Props<TEvent extends object, TResource extends object> extends EventCalendarParameters<TEvent, TResource> {
    children: React.ReactNode;
    /**
     * The store class to use for this provider.
     * @default EventCalendarStore
     */
    storeClass?: EventCalendarStoreConstructor<TEvent, TResource>;
  }
}