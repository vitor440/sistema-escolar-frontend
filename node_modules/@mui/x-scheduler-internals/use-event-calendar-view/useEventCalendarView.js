"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useEventCalendarView = useEventCalendarView;
var _useOnMount = require("@base-ui/utils/useOnMount");
var _useStore = require("@base-ui/utils/store/useStore");
var _useEventCalendarStoreContext = require("../use-event-calendar-store-context");
/**
 * Initializes the view on the event calendar.
 * The initialization is only done during the 1st render, the config should be defined statically.
 * ```
 * @param parameters Parameters for the view.
 */
function useEventCalendarView(config) {
  // Context hooks
  const store = (0, _useEventCalendarStoreContext.useEventCalendarStoreContext)();

  // Selector hooks
  const days = (0, _useStore.useStore)(store, config.visibleDaysSelector);

  // Feature hooks
  (0, _useOnMount.useOnMount)(() => store.setViewConfig(config));
  return {
    days
  };
}