import { SchedulerState as State } from "../internals/utils/SchedulerStore/SchedulerStore.types.js";
export declare const schedulerNowSelectors: {
  showCurrentTimeIndicator: (state: State<any>) => boolean;
  nowUpdatedEveryMinute: (state: State<any>) => Date;
  isCurrentDay: (args_0: State<any>, date: Date) => boolean;
};