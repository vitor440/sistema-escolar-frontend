import { SchedulerEventId } from "../models/index.mjs";
import { SchedulerState as State } from "../internals/utils/SchedulerStore/SchedulerStore.types.mjs";
export declare const schedulerOtherSelectors: {
  /**
   * Returns `true` if the event with the given ID is the currently active event.
   */
  isEditedEvent: (args_0: State<any>, eventId: SchedulerEventId | undefined) => boolean;
  visibleDate: (args_0: State<any>) => Date;
  isRecurringScopeDialogOpen: (state: State<any>) => boolean;
  /**
   * The default event color used when no color is specified on the event or its resource.
   */
  defaultEventColor: (state: State<any>) => import("../models/index.mjs").SchedulerEventColor;
  displayTimezone: (state: State<any>) => string;
  /**
   * Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
   */
  shouldEventRequireResource: (state: State<any>) => boolean;
  recurringEventsPlugin: (state: State<any>) => import("../internals/index.mjs").SchedulerRecurringEventsPluginInterface | null;
  areRecurringEventsAvailable: (state: State<any>) => boolean;
  isLoading: (state: State<any>) => boolean;
  errors: (state: State<any>) => readonly import("../internals/index.mjs").StoredError[];
};