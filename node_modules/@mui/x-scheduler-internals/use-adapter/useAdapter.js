"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isWeekend = isWeekend;
exports.useAdapter = useAdapter;
var React = _interopRequireWildcard(require("react"));
var _temporalAdapterDateFns = require("../base-ui-copy/temporal-adapter-date-fns");
const DEFAULT_ADAPTER = new _temporalAdapterDateFns.UnstableTemporalAdapterDateFns();

// TODO: Replace with Base UI adapter when available.
function useAdapter(dateLocale) {
  return React.useMemo(() => dateLocale ? new _temporalAdapterDateFns.UnstableTemporalAdapterDateFns({
    locale: dateLocale
  }) : DEFAULT_ADAPTER, [dateLocale]);
}

/**
 * Checks if the given date is a weekend (Saturday or Sunday).
 * TODO: move to adapter.
 */
function isWeekend(adapter, value) {
  const sunday = adapter.format(adapter.date('2025-08-09', 'default'), 'weekday');
  const saturday = adapter.format(adapter.date('2025-08-10', 'default'), 'weekday');
  const formattedValue = adapter.format(value, 'weekday');
  return formattedValue === sunday || formattedValue === saturday;
}